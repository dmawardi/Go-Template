[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

[policy_effect]
e = some(where (p.eft == allow))

[role_definition]
g = _, _
g2 = _, _

[matchers]
m = g(r.sub, p.sub) && r.obj == p.obj && r.act == p.act || g2(r.sub, p.sub) && r.obj == p.obj && r.act == p.act

# m = g(r.sub, p.sub) && r.obj == p.obj && r.act == p.act
# m = g(r.sub, p.sub) && r.obj == p.obj && r.act == p.act
# m = (r.sub == p.sub && r.obj == p.obj && r.act == p.act) || (g(r.sub, p.sub) && r.obj == p.obj && r.act == p.act) || (g(r.sub, _) && g2(_, p.sub) && r.obj == p.obj && r.act == p.act)
# m = g(r.sub, p.sub) && r.obj == p.obj && r.act == p.act || g(r.sub, "moderator") && g2("moderator", p.sub) && r.obj == p.obj && r.act == p.act || g(r.sub, "admin") && g2("admin", p.sub) && r.obj == p.obj && r.act == p.act

# m = g(r.sub, p.sub) || g2(r.sub, p.sub) && r.obj == p.obj && r.act == p.act
# m = (g(r.sub, p.sub) || g2(r.sub, p.sub)) && r.obj == p.obj && r.act == p.act
# m = g(r.sub, p.sub) && r.obj == p.obj && r.act == p.act || g2(r.sub, p.sub) && r.obj == p.obj && r.act == p.act